logging:
  level:
    by.Egrius.notification_service: DEBUG

server:
  port: 8081

spring:
  application:
    name: notification_service

  security:
    resourceserver:
      jwt:
        issuer-uri: http://auth-server.local:9000

  rabbitmq:
    username: guest
    password: guest
    host: localhost
    port: 5672

  datasource:
    url: jdbc:postgresql://localhost:5432/api_gateway_notifications_db
    username: postgres
    password: 2Pg8_06Egr
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        format_sql: true